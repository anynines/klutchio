apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: servicebindings.anynines.com
spec:
  group: anynines.com
  scope: Namespaced
  names:
    kind: ServiceBinding
    plural: servicebindings
  defaultCompositionRef:
    name: a8s-servicebinding
  versions:
    - name: v1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          supported:
            - types:
                &serviceInstanceTypes [
                  "keyvalue",
                  "postgresql",
                  "mongodb",
                  "search",
                  "logme2",
                  "prometheus",
                  "messaging",
                  "mariadb",
                  "messaging",
                ]
          properties:
            spec:
              x-kubernetes-validations:
                # Validation for crossplane.compositionRef and serviceInstanceType combination.
                - rule: "((has(self.crossplane) && has(self.crossplane.compositionRef) && has(self.crossplane.compositionRef.name) &&
                    self.crossplane.compositionRef.name != '' &&
                    self.crossplane.compositionRef.name != 'a8s-servicebinding') ||
                    self.serviceInstanceType == 'postgresql')"
                  message: "Selected service instance type is not supported."
              properties:
                acceptsIncomplete:
                  type: boolean
                instanceRef:
                  type: string
                writeConnectionSecretToRef:
                  description: Optional connection secret target requested by callers. Some compositions may ignore this value and generate their own secret names.
                  type: object
                  properties:
                    name:
                      type: string
                  required:
                    - name
                serviceInstanceType:
                  type: string
                  enum: *serviceInstanceTypes
              required:
                - instanceRef
                - serviceInstanceType
            status:
              type: object
              properties:
                managed:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true

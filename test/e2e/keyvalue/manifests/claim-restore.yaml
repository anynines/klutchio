apiVersion: anynines.com/v1
kind: Restore
metadata:
  name: restore-keyvalue-sample
  namespace: keyvalue-lifecycle
spec:
  backupRef: backup-keyvalue-sample
  serviceInstanceType: keyvalue
  # If no compositionRef is provided, the default choice will be a8s-restore.
  compositionRef:
    name: a9s-restore
